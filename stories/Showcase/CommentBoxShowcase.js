import React from "react";
import { CommentBox } from "../../src";

const CommentBoxStory = () => (
  <div>
   <CommentBox >@vivi Looks great! I will move this task to Completed. Whoop Whoop! :)</CommentBox>
  </div>
);

export default () => <CommentBoxStory />;
